import type { Meta } from '@storybook/react'
import { TableBody } from '../../components'

const meta = {
  title: 'App/Tables/TableBody',
  component: TableBody,
  tags: ['autodocs'],
} satisfies Meta<typeof TableBody>

export default meta
