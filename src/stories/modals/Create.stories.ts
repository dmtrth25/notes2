import type { Meta } from '@storybook/react'
import { CreateModal } from '../../components'

const meta = {
  title: 'App/Modals/CreateModal',
  component: CreateModal,
  tags: ['autodocs'],
} satisfies Meta<typeof CreateModal>

export default meta