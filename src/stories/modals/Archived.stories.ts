import type { Meta, StoryObj } from '@storybook/react'
import { Archived } from '../../components'

const meta = {
  title: 'App/Modals/Archived',
  component: Archived,
  tags: ['autodocs'],
} satisfies Meta<typeof Archived>

export default meta